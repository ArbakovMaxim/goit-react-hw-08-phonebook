import { WrapperForm } from './Login.styled';
import { LoginForm } from 'components/LoginForm/LoginForm';

const Login = () => {
  return (
    <WrapperForm>
      <LoginForm />
    </WrapperForm>
  );
};

export default Login;
