import { WrapperForm } from './Register.styled';

import { RegisterForm } from 'components/RegisterForm/RegisterForm';

const Register = () => {
  return (
    <WrapperForm>
      <RegisterForm />
    </WrapperForm>
  );
};

export default Register;
